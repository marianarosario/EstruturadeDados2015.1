#include <stdio.h>
#include <stdlib.h>

float ler(){
	float x;
	scanf("%f", &x);
	return x;
}

void imprimir(struct ponto arg){
	ptintf("%f", arg.x);
	ptintf("%f", arg.y);
}

void main(){
	struct ponto{
		float x;
		float y;
	};

	struct ponto v;
	v.x=ler();
	v.y=ler();	
	
	imprimir(v);

}
